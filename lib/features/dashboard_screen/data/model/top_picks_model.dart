class TopPicksModel{
    String imagePath;
      String time;
      String title;
      String subtitle;
  TopPicksModel({
    required this.imagePath,
    required this.subtitle,
    required this.time,
    required this.title
  });
}